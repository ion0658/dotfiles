gui:
    nerdFontsVersion: "3"

customCommands:
  - key: "C"
    context: "files"
    command: "git commit -v -t ~/.config/lazygit/commit_template_with_prompt.txt"
    description: "Commit changes using git editor"
    subprocess: true

